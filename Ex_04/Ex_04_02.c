#include <stdio.h>

int main(void) {
    int n;
    int m = 0;
    while (n != -1) {
        scanf("%d", &n);
        if (n >+ m) {
            m = n;
        }
    }
    printf("%d", m);
    return 0;
}
